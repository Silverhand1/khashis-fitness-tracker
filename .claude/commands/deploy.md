# Deploy to Vercel

Steps to deploy Khashi's Fitness Tracker:

1. Make sure the build passes: `npm run build`
2. If Vercel CLI is installed: `vercel --prod`
3. Otherwise push to GitHub — Vercel auto-deploys on push to main

Check the live URL after deploy and confirm the PWA manifest loads correctly.
